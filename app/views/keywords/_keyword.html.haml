%li
  = h keyword.query
  = link_to image_tag('action_remove.png'), keyword, :confirm => 'Are you sure?', :method => :delete
  = link_to pluralize(keyword.statuses.count, 'tweet'), '#', :class => 'unwrap'
  %ul.statuses
    = render keyword.statuses